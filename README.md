# Backup Manager - Менеджер бэкапов для Windows

Приложение для автоматического удаления устаревших файлов бэкапов с иконкой в системном трее и графическим интерфейсом настройки.

## 📋 Содержание

- [Описание](#описание)
- [Требования](#требования)
- [Установка Python](#установка-python)
- [Установка проекта](#установка-проекта)
- [Запуск приложения](#запуск-приложения)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Конфигурация](#конфигурация)
- [Логи](#логи)
- [Устранение неполадок](#устранение-неполадок)

## 📖 Описание

Backup Manager — это удобное приложение для Windows, которое автоматически отслеживает указанные папки с бэкапами и удаляет устаревшие файлы и папки согласно настраиваемым правилам. Приложение работает в фоновом режиме, имеет иконку в системном трее и предоставляет удобный графический интерфейс для настройки.

### Основные возможности:

- ✅ Мониторинг нескольких папок с бэкапами
- ✅ Настраиваемые правила удаления с использованием шаблонов (wildcard) или регулярных выражений
- ✅ Возможность оставить N самых свежих файлов/папок
- ✅ Выбор применения правила к конкретным папкам
- ✅ Удаление в корзину или навсегда (настраивается для каждого правила)
- ✅ Иконка в системном трее с меню
- ✅ Графический интерфейс для настройки
- ✅ Автозапуск при старте Windows
- ✅ Подробное логирование всех операций
- ✅ Визуальный конструктор регулярных выражений

## 🔧 Требования

- **ОС:** Windows 7/8/10/11
- **Python:** версия 3.7 или выше
- **Свободное место:** ~50 МБ для Python и зависимостей

## 🐍 Установка Python

Если Python ещё не установлен на вашем компьютере, следуйте этим инструкциям:

### Способ 1: Установка с официального сайта (рекомендуется)

1. Перейдите на официальный сайт Python: https://www.python.org/downloads/

2. Скачайте последнюю версию Python 3.x для Windows (например, Python 3.11 или 3.12)

3. Запустите установщик (`python-3.x.x-amd64.exe`)

4. **ВАЖНО:** В начале установки обязательно отметьте галочку **"Add Python to PATH"** (Добавить Python в PATH). Это позволит использовать Python из командной строки.

5. Нажмите "Install Now" (Установить сейчас) или "Customize installation" (Выборочная установка) для дополнительных настроек

6. Дождитесь завершения установки

7. Проверьте установку:
   - Откройте командную строку (Win + R, введите `cmd`, нажмите Enter)
   - Выполните команду:
     ```bash
     python --version
     ```
   - Должна отобразиться версия Python (например, `Python 3.11.5`)

8. Проверьте наличие `pip`:
   ```bash
   pip --version
   ```
   - Должна отобразиться версия pip (например, `pip 23.2.1`)

### Способ 2: Через Microsoft Store

1. Откройте Microsoft Store в Windows

2. Найдите "Python 3.11" или "Python 3.12"

3. Нажмите "Установить"

4. После установки Python будет автоматически добавлен в PATH

### Проверка установки

Откройте командную строку (PowerShell) и выполните:

```bash
python --version
pip --version
```

Если обе команды выводят версии, Python установлен правильно.

## 📦 Установка проекта

1. **Скачайте или клонируйте проект:**

   Если проект в Git репозитории:
   ```bash
   git clone <url-репозитория>
   cd backup_manager
   ```

   Или распакуйте архив с проектом в нужную папку

2. **Откройте командную строку (PowerShell) в папке проекта:**

   - Перейдите в папку проекта через Проводник
   - Нажмите Shift + Правый клик в папке
   - Выберите "Открыть окно PowerShell здесь" или "Открыть в Windows Terminal"

3. **Установите зависимости:**

   Выполните команду:
   ```bash
   pip install -r requirements.txt
   ```

   Это установит следующие библиотеки:
   - `PyQt5` — для графического интерфейса
   - `PyYAML` — для работы с конфигурационными файлами
   - `send2trash` — для удаления файлов в корзину Windows

   Установка займёт несколько минут в зависимости от скорости интернета.

4. **Проверьте установку зависимостей:**

   ```bash
   pip list
   ```

   Убедитесь, что в списке есть `PyQt5`, `PyYAML` и `send2trash`.

## 🚀 Запуск приложения

### Способ 1: Запуск с консолью (для отладки)

```bash
python main.py
```

При этом способе будет видно консольное окно с возможными ошибками.

### Способ 2: Запуск без консоли (рекомендуется)

```bash
pythonw launcher.pyw
```

Или просто дважды кликните на файл `launcher.pyw` в Проводнике Windows.

При этом способе приложение запустится без видимого консольного окна и будет работать в фоновом режиме.

### Первый запуск

При первом запуске:
- Приложение создаст папку `%USERPROFILE%\.backup_manager\` для хранения конфигурации
- Будет создан файл конфигурации `config.yaml` с настройками по умолчанию
- В системном трее появится иконка приложения

## 💻 Использование

### Доступ к настройкам

1. **Через двойной клик по иконке в трее:**
   - Найдите иконку Backup Manager в системном трее (область уведомлений)
   - Дважды кликните по иконке

2. **Через контекстное меню:**
   - Правый клик по иконке в трее
   - Выберите "Настройки"

### Настройка папок для мониторинга

1. Откройте окно настроек
2. Перейдите на вкладку "Папки"
3. Нажмите "Добавить папку"
4. Выберите папку с бэкапами, которую нужно отслеживать
5. Повторите для всех нужных папок

### Создание правил удаления

1. Перейдите на вкладку "Правила"
2. Нажмите "Добавить правило"
3. Заполните параметры правила:
   - **Название** — имя правила для удобства
   - **Тип паттерна** — шаблон (wildcard) или регулярное выражение
   - **Паттерн** — шаблон имени файла/папки (например, `*.bak` или `backup_*`)
   - **Удалять файлы старше** — возраст в днях
   - **Оставить самых свежих** — количество последних файлов, которые нужно сохранить (0 = удалить все подходящие)
   - **Тип удаления** — в корзину (по умолчанию) или навсегда
   - **Применять к папкам** — выберите, к каким отслеживаемым папкам применяется правило
   - **Правило активно** — включить/выключить правило

4. Нажмите "Сохранить"

### Примеры правил

- **Удалить все .bak файлы старше 30 дней:**
  - Тип: Шаблон (wildcard)
  - Паттерн: `*.bak`
  - Удалять старше: `30` дней
  - Оставить самых свежих: `0`

- **Оставить только 10 последних SQL бэкапов:**
  - Тип: Шаблон (wildcard)
  - Паттерн: `*.sql`
  - Удалять старше: `1` день
  - Оставить самых свежих: `10`

- **Удалить папки по шаблону:**
  - Тип: Регулярное выражение
  - Паттерн: `^backup_\d{4}-\d{2}-\d{2}$`
  - Удалять старше: `7` дней

### Ручная очистка

- Правый клик по иконке в трее
- Выберите "Очистить сейчас"
- Приложение выполнит очистку согласно всем активным правилам
- В системном уведомлении появится сообщение о результатах

### Автозапуск

1. Откройте настройки (вкладка "Общие")
2. Отметьте "Запускать автоматически при старте Windows"
3. Нажмите "Сохранить настройки"
4. Приложение будет автоматически запускаться при входе в Windows

### Выход из приложения

- Правый клик по иконке в трее
- Выберите "Выход"

## 📁 Структура проекта

```
backup_manager/
├── main.py              # Точка входа приложения
├── launcher.pyw         # Запуск без консоли
├── backup_manager.py    # Основная логика работы с файлами
├── config_manager.py    # Управление конфигурацией
├── gui.py               # Графический интерфейс
├── tray_icon.py         # Иконка в системном трее
├── logger.py            # Система логирования
├── requirements.txt     # Список зависимостей
└── README.md            # Этот файл
```

## ⚙️ Конфигурация

Конфигурация хранится в файле:
```
%USERPROFILE%\.backup_manager\config.yaml
```

Например:
```
C:\Users\ВашеИмя\.backup_manager\config.yaml
```

Файл создаётся автоматически при первом запуске. Вы можете редактировать его вручную, но рекомендуется использовать графический интерфейс.

### Структура конфигурации

```yaml
watch_folders:
  - "C:\\Path\\To\\Backups"
  - "D:\\Backups"

rules:
  - name: "Удалить файлы старше 30 дней"
    pattern: "*"
    pattern_type: "wildcard"
    max_age_days: 30
    enabled: true
    folders: []  # Пустой список = правило не применяется ни к одной папке
    keep_latest: 0  # 0 = удалить все подходящие
    permanent_delete: false  # false = в корзину, true = навсегда

check_interval_seconds: 3600  # Интервал проверки в секундах (1 час)
auto_start: false  # Автозапуск при старте Windows
```

## 📝 Логи

Все операции приложения логируются в файл:
```
%USERPROFILE%\.backup_manager\logs\backup_manager.log
```

Например:
```
C:\Users\ВашеИмя\.backup_manager\logs\backup_manager.log
```

### Что логируется:

- Начало и завершение сканирования
- Удаление файлов и папок (с указанием типа удаления)
- Все ошибки с полными деталями
- Обработка каждой отслеживаемой папки
- Проблемы с конфигурацией и автозапуском

### Ротация логов:

- Максимальный размер файла: 5 МБ
- Количество архивных файлов: 5
- При достижении лимита создаётся новый файл, старые архивируются

Путь к файлу логов также отображается на вкладке "Общие" в настройках приложения.

## 🔍 Устранение неполадок

### Приложение не запускается

1. **Проверьте версию Python:**
   ```bash
   python --version
   ```
   Должна быть версия 3.7 или выше

2. **Проверьте установку зависимостей:**
   ```bash
   pip list
   ```
   Убедитесь, что установлены: `PyQt5`, `PyYAML`, `send2trash`

3. **Переустановите зависимости:**
   ```bash
   pip install --upgrade -r requirements.txt
   ```

4. **Проверьте логи:**
   Откройте файл `%USERPROFILE%\.backup_manager\logs\backup_manager.log`

### Иконка не появляется в трее

- Проверьте область уведомлений (может быть скрыта)
- Перезапустите приложение
- Проверьте, что приложение действительно запущено (через Диспетчер задач)

### Файлы удаляются навсегда вместо корзины

1. **Проверьте установку send2trash:**
   ```bash
   pip install send2trash
   ```

2. **Проверьте настройки правила:**
   - Откройте правило в настройках
   - Убедитесь, что галочка "Удалять навсегда (без корзины)" **не отмечена**

3. **Проверьте логи** на наличие ошибок

### Ошибка импорта PyQt5

```bash
pip install --upgrade PyQt5
```

Если не помогает:
```bash
pip uninstall PyQt5
pip install PyQt5==5.15.10
```

### Ошибка "ModuleNotFoundError"

Убедитесь, что вы установили все зависимости:
```bash
pip install -r requirements.txt
```

### Приложение не запускается автоматически

1. Проверьте настройку автозапуска в приложении (вкладка "Общие")
2. Проверьте, что файл `launcher.pyw` существует
3. Проверьте права доступа к реестру Windows
4. Посмотрите логи на наличие ошибок автозапуска

### Проблемы с правами доступа

Если приложение не может удалить файлы:
- Убедитесь, что у пользователя есть права на удаление файлов в отслеживаемых папках
- Проверьте, не открыты ли файлы в других программах
- Посмотрите логи для деталей ошибки

## 📞 Поддержка

При возникновении проблем:
1. Проверьте файл логов
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Python (должна быть 3.7+)

## 📄 Лицензия

[Укажите лицензию, если необходимо]

## 🎯 Планы развития

- [ ] Мониторинг в реальном времени (watchdog)
- [ ] Уведомления при удалении файлов
- [ ] Экспорт/импорт конфигурации
- [ ] Статистика и отчёты
- [ ] Поддержка других операционных систем

---

**Приятного использования!** 🚀
